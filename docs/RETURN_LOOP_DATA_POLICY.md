# Return-loop data policy

A tournament or offer is publishable only when it is attributable to the relevant real club and backed by an HTTPS source. `official_instagram`, `official_website`, and `owner_submission` are preferred. `other` requires manual verification before publication. Expired or deactivated items are not public.
