import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';

const mobileNav = await readFile(new URL('../src/components/navigation/MobileNav.tsx', import.meta.url), 'utf8');
const searchFilter = await readFile(new URL('../src/components/filters/SearchFilter.tsx', import.meta.url), 'utf8');
const layout = await readFile(new URL('../src/app/layout.tsx', import.meta.url), 'utf8');

assert.ok(mobileNav.includes('href="/#club-search"'), 'Mobile search navigation must keep the #club-search destination');
assert.ok(mobileNav.includes('fixed inset-x-0 bottom-0'), 'Mobile navigation must keep a CSS fixed-bottom fallback.');
assert.ok(!mobileNav.includes('[transform:translateZ(0)]'), 'Mobile navigation must not keep a transformed compositor layer that can retain stale keyboard geometry on iOS.');
assert.ok(mobileNav.includes('window.visualViewport'), 'Mobile navigation must observe the real visual viewport on mobile browsers.');
assert.ok(mobileNav.includes('isIOSWebKit'), 'Mobile navigation must isolate the iOS/WebKit workaround to affected devices.');
assert.ok(mobileNav.includes("nav.style.position = 'absolute'"), 'iOS navigation must bypass the broken position:fixed layer entirely.');
assert.ok(mobileNav.includes('getMobileNavDocumentTop'), 'iOS navigation must follow visualViewport.pageTop in document coordinates.');
assert.ok(mobileNav.includes('visualViewport.pageTop'), 'iOS absolute navigation must use the visual viewport document position.');
assert.ok(mobileNav.includes("nav.style.bottom = 'auto'"), 'iOS absolute mode must stop relying on fixed bottom anchoring.');
assert.ok(!mobileNav.includes('window.innerHeight'), 'Mobile navigation must not use stale layout viewport height for iOS positioning.');
assert.ok(!mobileNav.includes('getMobileNavVisualTop'), 'The failed fixed-top visual viewport workaround must stay removed.');
assert.ok(mobileNav.includes('isPhantomBottomScroll'), 'iOS navigation must detect browser-created bottom scroll overflow.');
assert.ok(mobileNav.includes('getRealLayoutMaxScrollTop'), 'iOS navigation must compute the real maximum layout scroll from content and the visual viewport.');
assert.ok(mobileNav.includes('isPhantomBottomScroll(window.scrollY, maxLayoutScrollTop)'), 'Phantom detection must use layout scroll rather than visualViewport.pageTop.');
assert.ok(mobileNav.includes("window.scrollTo({ top: maxLayoutScrollTop, left: 0, behavior: 'auto' })"), 'Phantom iOS bottom scrolling must clamp directly in layout scroll coordinates.');
assert.ok(layout.includes('data-mobile-content-end="true"'), 'Root layout must expose a sentinel for the real mobile content end.');
assert.ok(mobileNav.includes("document.addEventListener('focusin', handleFocusIn, true)"), 'Bottom navigation must observe iOS editable focus.');
assert.ok(mobileNav.includes("if (!isEditableElement(event.target)) return;"), 'Keyboard handling must ignore links, buttons and other non-editable focus targets.');
assert.ok(mobileNav.includes("document.addEventListener('focusout', handleFocusOut, true)"), 'Bottom navigation must settle after the iOS keyboard closes.');
assert.ok(mobileNav.includes('let editableFocusSeen = false'), 'Keyboard recovery must remember that editable focus was observed even if focusout is missed.');
assert.ok(mobileNav.includes('let keyboardPollTimer = 0'), 'Keyboard recovery must include an activeElement watchdog.');
assert.ok(mobileNav.includes('keyboardPollTimer = window.setTimeout'), 'Editable focus must schedule independent polling so missed focusout events cannot strand the nav.');
assert.ok(mobileNav.includes('}, 100);'), 'Editable focus watchdog must recheck promptly while the input remains focused.');
assert.ok(mobileNav.includes('let keyboardReleaseTimer = 0'), 'Keyboard dismissal must use a dedicated release timer that viewport resize cannot cancel.');
assert.ok(mobileNav.includes('keyboardReleaseTimer = window.setTimeout'), 'Keyboard dismissal must schedule an independent release.');
assert.ok(mobileNav.includes('editableFocusSeen = false;'), 'Keyboard release must explicitly clear editable-focus recovery state.');
assert.ok(mobileNav.includes('}, 500);'), 'Keyboard release must wait for delayed iOS viewport restoration before showing the nav.');
assert.ok(mobileNav.includes("window.addEventListener('pageshow', settleViewport)"), 'Mobile navigation must re-anchor after iOS page-cache restores.');
assert.ok(mobileNav.includes("document.addEventListener('visibilitychange', handleVisibilityChange)"), 'Mobile navigation must re-anchor when the browser tab becomes visible again.');
assert.ok(mobileNav.includes('}, [pathname]);'), 'Mobile navigation must re-anchor after every client-side route transition.');
assert.ok(searchFilter.includes('a[href="/#club-search"]'), 'SearchFilter must intercept same-page mobile search navigation');
assert.ok(searchFilter.includes('event.preventDefault()'), 'Same-page mobile search navigation must prevent the no-op hash navigation');
assert.ok(searchFilter.includes("document.addEventListener('click', handleMobileSearchNavigation, true)"), 'Search navigation interception must run in capture phase before Next Link routing');
assert.ok(searchFilter.includes("document.removeEventListener('click', handleMobileSearchNavigation, true)"), 'Search navigation capture listener must be removed cleanly');
assert.ok(searchFilter.includes("window.history.replaceState(window.history.state, '', '#club-search')"), 'Search navigation must keep the canonical #club-search hash');
assert.ok(searchFilter.includes("document.getElementById('club-search')"), 'Search navigation must resolve the search container');
assert.ok(searchFilter.includes("scrollIntoView({ behavior: 'smooth', block: 'start' })"), 'Search navigation must visibly move the search controls into view');
assert.ok(searchFilter.includes('inputRef.current?.focus({ preventScroll: true })'), 'Search navigation must focus the search input for immediate typing');
assert.ok(searchFilter.includes("window.location.hash === '#club-search'"), 'SearchFilter must focus after cross-page navigation arrives on the hash');

console.log('Mobile search navigation regression passed.');
